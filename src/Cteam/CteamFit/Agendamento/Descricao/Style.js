import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 10,
      },
      input: {
        height: 40,
        width:350,
        borderColor: '#000',
        borderWidth: 1,
        paddingHorizontal: 8,
        marginTop: 8,
        marginLeft:8,
      },
      Texto:{
        fontWeight:"bold",
        fontSize:20,
        alignItems:"center",
        paddingLeft:6,
        paddingRight:15
      }

})

export default styles