import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    Texto:{
        color: "#",
        fontWeight:"bold",
        fontSize:30,
        height:100
    },
    Conteiner:{
        alignItems: "center"
    }

})

export default styles